rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DESENVOLVIMENTO: Qualquer usuário autenticado pode ler/escrever
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
